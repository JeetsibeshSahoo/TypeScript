1 - Type Card, known, unknown, any
2 - Difference between any and unknown
3 - Type Narrowing

unknown -: The any type is the most-capable type in TS - while it encompasses the type of every possible value, it doesn't force us to do any checking before we try to call, construct, or access properties on these values. It also lets us assign values of type any to values that expect any other type.

4 - type - never (never type) 2 use cases
5 - type assertion

types type elise
interfaces
basic union intersection